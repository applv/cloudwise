server:
  port: 8082
spring:
  application:
    name: Cloudwise
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:ApplicationsLibrary;NON_KEYWORDS=USER
    driver-class-name: org.h2.Driver
    username: user
    password:
  sql:
    init:
      schema-locations: classpath:db/schema.sql
      mode: always
web:
  index-url: /swagger-ui/index.html